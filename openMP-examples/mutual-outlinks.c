#include <omp.h>
#include <math.h>
#include <stdio.h>
#include <stdlib.h>

int main(int argc, char** argv) {
	int n2 = n/2, i, j;
	int n = atoi(argv[1]); 
	int msize = n * n * sizeof(int); 

	// for (int i = 0; i < n; i++) {
	// 	m[n*i + i] = 0;
	// }
}